orientation: normal
rows: 1
columns: 3
knobs: 1
layers:
  - buttons:
      - ["t", "shift-leftbracket", "shift-rightbracket"]   # Trim edit mode / trim start (ripple) / trim end (ripple)
    knobs:
      - ccw: "left"        # Nudge clip one frame left
        press: "t"         # Toggle trim edit mode
        cw: "right"        # Nudge clip one frame right
